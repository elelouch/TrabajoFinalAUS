@page "/testing"
@rendermode InteractiveServer

<input @bind="text"/>
<button class="btn btn-primary" @onclick="() => text = string.Empty">Clear</button>

<input @onchange="InputChanged" />
<p>@message</p>

@code {
    string message = "";
    string text = "";


    void InputChanged(ChangeEventArgs e)
    {
        Console.WriteLine(e);
        message = (string)e.Value;
    }
}